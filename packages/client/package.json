{
  "name": "@cypheria/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./internal/server-client": "./src/server-client.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm typecheck",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@cypheria/protocol": "workspace:*",
    "@cypheria/relay": "workspace:*",
    "semver": "^7.8.5"
  },
  "devDependencies": {
    "@types/semver": "^7.8.0",
    "vitest": "^4.1.7"
  }
}
