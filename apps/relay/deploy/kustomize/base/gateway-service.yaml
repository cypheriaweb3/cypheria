apiVersion: v1
kind: Service
metadata:
  name: cypheria-relay-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cypheria-relay
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 80
      targetPort: public
      protocol: TCP
